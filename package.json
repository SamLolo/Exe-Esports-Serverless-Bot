{
  "name": "serverless-discord",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "azure-functions-core-tools": "^3.0.5682",
    "slash-create": "^6.2.1"
  },
  "devDependencies": {
    "@azure/functions": "^3.5.1",
    "@types/node": "^20.x",
    "eslint": "^9.9.1",
    "rimraf": "^5.0.0",
    "typescript": "^4.9.5"
  }
}
